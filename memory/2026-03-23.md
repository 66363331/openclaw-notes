
## 🔒 夜间安全审计 (12:51 UTC)

**OpenClaw:** 2026.3.13 | Channel: stable | ✅ 最新

**审计结果:** 0 critical · 2-3 warn · 1 info

### ✅ 无严重问题

**攻击面:**
- Gateway 仅监听 loopback (127.0.0.1:18789/18791/18792) — 无外部暴露
- 允许列表模式: open=0, allowlist=1
- hooks.webhooks: disabled
- hooks.internal: disabled
- trust model: personal assistant (单可信操作员边界)

**工具状态:**
- tools.elevated: enabled (预期行为)
- browser control: enabled (预期行为)

### ⚠️ 警告项

1. **trusted_proxies_missing** — Gateway 绑定在 loopback，代理信任配置空。无需操作（仅本地访问）。
2. **feishu accounts.default missing** — 飞书渠道配置缺少默认账号绑定，如使用飞书功能需配置 `channels.feishu.accounts.default`。
3. **gateway probe_failed (deep)** — 缺少 `operator.read` 权限范围，可尝试 `openclaw status --all` 重连认证。

### 操作
- 建议：如使用飞书工具，检查 Feishu 账号配置。
