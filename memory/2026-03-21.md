# 2026-03-21 Nightly Security Audit

## Time: 2026-03-21 03:00 AM (Asia/Shanghai)

## OpenClaw Version/Update Status
- Gateway running normally (localhost:18789)
- Update status: check `openclaw update status` for channel info

## Security Audit Results

### ❌ CRITICAL (3)
1. **open_groups_with_elevated** — Feishu groupPolicy="open" + tools.elevated enabled
   - Risk: prompt injection in Feishu groups → command execution with elevated privileges
   - Fix: Set `groupPolicy="allowlist"` or enable `tools.profile="messaging"`

2. **open_groups_with_runtime_or_fs** — open groupPolicy + exec/write/fs tools exposed
   - Risk: sandbox=off, runtime=[exec,process], fs=[read,write,edit], workspaceOnly=false
   - Fix: `tools.fs.workspaceOnly=true`, `agents.defaults.sandbox.mode="all"`

3. **Feishu channel warning** — groupPolicy="open" allows any member to trigger
   - Fix: `channels.feishu.groupPolicy="allowlist"` + `groupAllowFrom` restriction

### ⚠️ WARN (4)
1. **trusted_proxies_missing** — gateway.bind=loopback but trustedProxies empty
   - Impact: reverse proxy spoofing possible if Control UI exposed
   - Fix: set `gateway.trustedProxies` or keep Control UI local-only

2. **Feishu doc_owner_open_id** — feishu_doc create grants requester permissions
   - Fix: disable `channels.feishu.tools.doc` if not needed

3. **multi_user_heuristic** — potential multi-user setup detected
   - Open groupPolicy + untrusted users = security boundary risk
   - Fix: split trust boundaries if users mutually untrusted

4. **gateway.probe_failed** — missing scope `operator.read`
   - Fix: run `openclaw status --all`

### ℹ️ INFO
- Gateway listening on 127.0.0.1:18789/18791/18792 (local only, good)
- tools.elevated: enabled
- browser control: enabled
- trust model: personal assistant (not multi-tenant)
- groups: open=0, allowlist=1

## Listening Ports
- 127.0.0.54:53 — DNS stub
- 10.255.255.254:53 — DNS
- 127.0.0.1:18789/18791/18792 — openclaw-gateway (local only, good)

## Recommended Actions (优先级排序)
1. 🔴 Fix Feishu groupPolicy="open" 问题（最高优先）
2. 🔴 设置 `tools.fs.workspaceOnly=true`
3. 🟡 配置 `agents.defaults.sandbox.mode="all"`
4. 🟡 设置 `gateway.trustedProxies`（如有反代）
5. 🟢 运行 `openclaw status --all` 排查 probe 失败
